name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Script
        run: cmd /c src\main.cmd

      - name: Build
        run: pwsh scripts/build.ps1